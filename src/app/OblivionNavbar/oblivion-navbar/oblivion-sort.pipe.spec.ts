import { OblivionSortPipe } from './oblivion-sort.pipe';

describe('OblivionSortPipe', () => {
  it('create an instance', () => {
    const pipe = new OblivionSortPipe();
    expect(pipe).toBeTruthy();
  });
});
